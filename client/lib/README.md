# DownFlux Lib
----

This directory is for all classes which do not explicitly rely on the Godot
runtime, e.g. no subclasses of `Godot.Node`. The intent of this folder is to
store all classes which may be tested with self-contained unit and performance
tests.